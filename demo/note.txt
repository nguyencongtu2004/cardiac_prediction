Chuẩn bị dữ liệu

Load file synthetic_cardiac_5000.csv

Làm sạch, chọn feature

Chia train/valid/test

Feature Engineering offline

Chuẩn hóa (StandardScaler)

Encode binary feature

Tạo vector đầu vào

Train mô hình

Logistic Regression (baseline)

XGBoost / LightGBM / RandomForest (model chính)

Đánh giá

AUC, F1, Precision-Recall

Quan trọng để đưa vào báo cáo

Demo predict

Tạo một hàm predict(patient_record)

Demo nhập vào vài dòng → Model trả về “nguy cơ nhập viện tim mạch 0/1”

